# Talks

One Markdown file per talk. The frontmatter drives the card on `/talks` and the
detail page at `/talks/<file-name>`; the body is the abstract shown on that
page. See the "Talks" section of `docs/CUSTOMIZATION.md` for the full guide.

Files matching `*README.md` are ignored by the collection loader.
